Tests are contained in automation.py

Prerequisites:
These must be downloaded and installed onto the machine running the tests.
Python3
chromedriver

Edit:
Line 12 with the path to the chromedriver

Install packages:
pip3 install -r requirements.txt

Run:
python3 automation.py
